# User Management System (JDBC & Hibernate)

Консольное приложение для управления пользователями в базе данных, реализующее CRUD-операции.

Главная цель проекта — изучить и сравнить два подхода к работе с БД: нативный **JDBC** и ORM-фреймворк **Hibernate**.

## 📋 Функционал

Приложение реализует слой DAO (Data Access Object) и позволяет выполнять следующие операции:
- Создание таблицы пользователей в БД.
- Добавление нового пользователя.
- Удаление пользователя по ID.
- Получение списка всех пользователей.
- Очистка таблицы.
- Удаление таблицы.

Архитектура приложения построена на разделении слоев: `Main` -> `Service` -> `DAO` -> `Database`.

## 🛠 Технологический стек

*   **Java 24** — основной язык разработки.
*   **Maven** — сборка проекта и управление зависимостями.
*   **MySQL 8** — система управления базами данных.
*   **Hibernate 5.6** — ORM-фреймворк для работы с БД через объекты.
*   **JDBC** — нативное взаимодействие с базой данных.
*   **JUnit 4** — unit-тестирование методов.

## 🚀 Как запустить проект

### Предварительные требования
1.  Установленный **JDK 8** или выше.
2.  Установленный **Maven**.
3.  Запущенный сервер **MySQL**.

### Шаг 1. Клонирование репозитория
```bash
git clone https://github.com/Engelsky/projectKataJDBC

**### Шаг 2. Настройка базы данных**
Перед запуском необходимо создать базу данных в MySQL и настроить подключение.

Откройте терминал MySQL или Workbench и выполните команду:
CREATE DATABASE kata_users_db;

- Откройте проект в IntelliJ IDEA.

- Перейдите в файл src/main/java/jm/task/core/jdbc/util/Util.java.

- Укажите ваши параметры подключения (URL, логин и пароль):
private static final String DB_URL = "jdbc:mysql://localhost:3306/katapp1134";
private static final String DB_USER = "root";
private static final String DB_PASSWORD = "ваш_пароль";

**### Шаг 3. Запуск**

Вариант А. Через IDE:
Откройте класс Main.java и запустите метод main(). В консоли будет выведен лог работы программы (создание таблицы, добавление юзеров, вывод списка и удаление).

Вариант Б. Запуск тестов:
Чтобы проверить корректность работы всех методов, запустите класс UserServiceTest.java.

🔄 Переключение между JDBC и Hibernate
В проекте реализованы две версии DAO. Чтобы переключиться между ними, измените реализацию в классе UserServiceImpl.java:

Для использования Hibernate:
private UserDao userDao = new UserDaoHibernateImpl();

Для использования чистого JDBC:
private UserDao userDao = new UserDaoJDBCImpl();

***

### Пару советов:
1.  В разделе "Настройка базы данных" я написал ссылку на свой репозиторий — если у тебя своя БД, то проверь, как база называется у тебя в `Util` классе.
2.  Тесты лучше запустить через панель Maven в IDE Lifecycle -> test
